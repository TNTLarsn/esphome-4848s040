button:
  - platform: restart
    id: restart_button
    name: "Restart"
    entity_category: config

  - platform: template
    name: "Check for Updates"
    id: check_updates_button
    entity_category: config
    icon: "mdi:update"
    on_press:
      then:
        - component.update: firmware_update