# ESPHome 4848S040 Display - Hauptkonfiguration
# Diese Datei importiert alle modularen Pakete

packages:
  # Core-Komponenten
  base: !include packages/core/base.yaml
  wifi: !include packages/core/wifi.yaml
  api: !include packages/core/api.yaml
  updates: !include packages/core/updates.yaml

  # Sensoren
  diagnostics: !include packages/sensors/diagnostics.yaml
  system_info: !include packages/sensors/system_info.yaml

  # Buttons
  system_buttons: !include packages/buttons/system.yaml

  # Integrationen (hier weitere hinzufügen)
  # display: !include packages/integrations/display.yaml
  # custom_sensors: !include packages/integrations/custom_sensors.yaml